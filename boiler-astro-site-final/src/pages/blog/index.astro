
---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';
const posts = await getCollection('blog');
---
<BaseLayout title="Blog" schema={{"@context":"https://schema.org","@type":"Blog"}}>
<main class="page">
<h1>Blog</h1>
<ul>
{posts.map(p => <li><a href={`/blog/${p.slug}/`}>{p.data.title}</a> – {p.data.category}</li>)}
</ul>
</main>
</BaseLayout>
